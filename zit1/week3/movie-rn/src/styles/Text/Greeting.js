import React from "react";
import { StyleSheet } from "react-native";

// Import colors
import { gray600 } from '../../themes/Colors';

const styles = StyleSheet.create({
    greeting: {
      fontWeight: '400',
      fontSize: 16,
      color: gray600,
      marginBottom: 6
    },
});

export default styles;